import React from "react"
import { Text } from "@react-email/components"

const Disclaimer = () => (
  <Text className="text-xs leading-normal text-neutral-400">{`If you didn't request this email, there's nothing to worry about, you can safely ignore it.`}</Text>
)

export default Disclaimer
